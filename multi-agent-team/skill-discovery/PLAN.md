# Multi-Agent Team Skill 修改方案

## 目标
增强 multi-agent-team 技能，使其能够：
1. 自动发现当前可用的 skills
2. 在任务规划时考虑可用 skills
3. 在分配任务时明确告知子智能体要使用什么 skill 及使用方案

---

## 当前问题

1. PM 规划任务时不知道有哪些 skills 可用
2. 用户指定 skill 时没有机制来传递这个信息
3. 子智能体执行任务时不知道可以使用哪些工具

---

## 修改方案

### 1. 新增 Skill Discovery 机制

#### 1.1 Skill 扫描功能
**位置**: 在 PM 初始化阶段执行
**方法**: 读取 `~/clawd/skills/` 目录，获取所有已安装 skill 的元数据

```
skill-discovery/
├── scan-skills.js      # 扫描技能目录的脚本
├── skill-index.json    # 生成的技能索引缓存
└── README.md           # 使用说明
```

**scan-skills.js 功能**:
- 遍历 `~/clawd/skills/` 目录
- 读取每个 skill 的 SKILL.md
- 提取: name, description, usage patterns, dependencies
- 生成 skill-index.json

#### 1.2 Skill 索引缓存
**skill-index.json 结构**:
```json
{
  "generatedAt": "2026-02-01T13:00:00Z",
  "skills": [
    {
      "name": "nano-banana-pro",
      "description": "Generate/edit images with Nano Banana Pro",
      "usage": "Use when image creation requests include 'create image', 'generate image'",
      "location": "/Users/xdrshjr/clawd/skills/nano-banana-pro",
      "capabilities": ["image-generation", "image-editing"],
      "priority": "high"
    }
  ]
}
```

### 2. 修改 PM 规划流程

#### 2.1 任务分析阶段增强

**新增步骤**: 在 PM 分析用户请求时：
1. 检查用户是否明确指定了 skill（关键词: "使用xx技能", "用xx", "调用xx"）
2. 加载 skill-index.json
3. 根据任务类型匹配合适的 skills
4. 生成 Skill 使用建议

**匹配逻辑**:
```
if (user_request 包含 "视频") → 推荐: hf-papers-to-video, remotion-synced-video, video-transcript-downloader
if (user_request 包含 "图片/图像") → 推荐: nano-banana-pro, google-images-crawler
if (user_request 包含 "论文") → 推荐: hf-papers-reporter, paper-daily
```

#### 2.2 项目规划文档增强

**projects/{project-id}.md** 新增章节:
```markdown
## Skill 使用规划

### 用户指定
- 用户要求使用: [skill-name]
- 原因: [optional user reason]

### PM 推荐
| Skill | 用途 | 分配给 | 优先级 |
|-------|------|--------|--------|
| nano-banana-pro | 生成封面图 | Designer Agent | High |
| remotion | 视频渲染 | Video Agent | High |

### 备选 Skills
- [skill-name]: [备选场景]
```

### 3. 修改子智能体任务分配

#### 3.1 任务 Prompt 模板增强

**当前任务分配格式**:
```
你是一个{role}，负责{task}。
```

**增强后任务分配格式**:
```
你是一个{role}，负责{task}。

═══════════════════════════════════════════════════════════
🛠️ 可用工具与技能
═══════════════════════════════════════════════════════════

本任务可以使用以下 skills:

【Skill: {skill-name}】
• 功能: {description}
• 使用场景: {when-to-use}
• 使用方法: {how-to-use}
• 示例:
  ```
  {usage-example}
  ```

【Skill: {skill-name-2}】
...

⚠️ 重要提示:
1. 在执行任务前，先检查是否有可用的 skill
2. 优先使用 skill 而不是手动实现
3. 如果 skill 不能满足需求，立即向 PM 汇报

═══════════════════════════════════════════════════════════
```

#### 3.2 特定 Skill 使用指南

**针对每个常用 skill，准备使用指南**:

```
skills/
└── multi-agent-team/
    └── skill-guides/
        ├── nano-banana-pro.md      # 图像生成技能使用指南
        ├── hf-papers-reporter.md   # 论文报告技能使用指南
        ├── remotion-synced-video.md # 视频生成技能使用指南
        └── template.md             # 技能指南模板
```

**skill-guide 模板结构**:
```markdown
# {skill-name} 使用指南

## 功能概述
简要描述这个 skill 能做什么

## 何时使用
- 场景1: [具体描述]
- 场景2: [具体描述]

## 使用方法
### 方法1: [方法名称]
```bash
{command-example}
```
或
```
{conversation-example}
```

## 常见参数
| 参数 | 说明 | 示例 |
|------|------|------|
| param1 | 说明 | value |

## 注意事项
- 注意1
- 注意2

## 故障排除
| 问题 | 解决方案 |
|------|----------|
| 问题1 | 解决方案 |
```

### 4. 用户指定 Skill 的处理机制

#### 4.1 识别用户意图

**关键词检测**:
```
"使用xx技能" → extract skill name
"用xx来做" → extract skill name  
"调用xx" → extract skill name
"基于xx" + skill name → extract skill name
```

#### 4.2 Skill 验证

**验证步骤**:
1. 检查 skill 是否存在于 skill-index.json
2. 检查 skill 是否适合当前任务类型
3. 如果不适合，向用户确认或建议替代方案

#### 4.3 强制使用标记

**在项目状态中标记**:
```json
{
  "userPreferences": {
    "mandatorySkills": ["nano-banana-pro"],
    "preferredSkills": ["remotion"],
    "excludedSkills": []
  }
}
```

### 5. 实施步骤

#### Phase 1: 基础功能 (优先级: 高)
1. [ ] 创建 skill-discovery/scan-skills.js
2. [ ] 实现 skill-index.json 生成
3. [ ] 在 PM 初始化时加载 skill 索引

#### Phase 2: 规划增强 (优先级: 高)
4. [ ] 修改任务分析逻辑，添加 skill 匹配
5. [ ] 实现用户指定 skill 的识别和验证
6. [ ] 更新项目规划文档模板

#### Phase 3: 任务分配增强 (优先级: 中)
7. [ ] 创建 skill-guides/ 目录和模板
8. [ ] 为常用 skills 编写使用指南
9. [ ] 修改任务分配 prompt，包含 skill 信息

#### Phase 4: 测试与优化 (优先级: 中)
10. [ ] 编写测试用例
11. [ ] 验证 skill 发现和匹配逻辑
12. [ ] 验证任务分配中的 skill 说明

---

## 预期效果

1. **PM 规划更智能**: 自动考虑可用工具，给出更合理的任务分配
2. **子智能体执行更高效**: 明确知道可以使用哪些 skills，减少重复劳动
3. **用户意图更精准**: 用户指定的 skill 会被优先使用
4. **整体效率提升**: 减少 PM 和子智能体之间的反复确认

---

## 可能的问题与解决方案

| 问题 | 解决方案 |
|------|----------|
| Skill 索引过期 | 每次启动时重新扫描，或添加缓存时间戳检查 |
| Skill 描述不准确 | 标准化 SKILL.md 格式，要求包含 usage 部分 |
| 用户指定的 skill 不适合 | PM 验证后向用户确认，提供替代方案 |
| 子智能体不会使用 skill | 提供详细的使用指南和示例 |
| Skill 依赖缺失 | 在 skill 索引中标记依赖，PM 检查时预警 |

---

## 下一步行动

请确认这个方案后，我将开始实施 Phase 1 的开发。

需要您决策的点:
1. 是否同意这个方案的整体方向？
2. 是否有特定的 skills 需要优先支持？
3. 是否需要调整任何部分的优先级？
